import { Helmet } from "react-helmet-async";

const TodosPage = () => {
  return (
    <div>
      <Helmet>
        <title>Todos</title>
      </Helmet>
      TodosPage
    </div>
  );
};

export default TodosPage;
